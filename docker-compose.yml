# 仓库管理系统 Docker Compose 配置
name: warehouse-management

services:
  # 仓库管理系统前端
  frontend:
    image: docker.cnb.cool/wtrw09/warehouse/warehouse-frontend:latest
    ports:
      - "8081:80" 
    environment:
      - NODE_ENV=production
      # 禁用开发工具和热重载
      - VITE_DEV_TOOLS=false
      - VITE_HMR=false
    volumes:
      # 挂载整个config目录（支持容器内自动创建文件）
      - ./config:/etc/nginx/conf-external
      # 挂载nginx日志目录,方便查看日志
      - ./logs/nginx:/var/log/nginx
    restart: unless-stopped
    depends_on:
      warehouse-backend:
        condition: service_healthy
    networks:
      - warehouse-network

  # 仓库管理系统后端
  warehouse-backend:
    image: docker.cnb.cool/wtrw09/warehouse/warehouse-backend:latest
    #移除端口映射，后端只能在容器内部访问
    #ports:
      #- "8000:8000"
    environment:
      - WORKERS=1  # SQLite数据库，必须使用单进程模式避免锁冲突
    volumes:
      - ./warehouseBackend/data:/app/data
      - ./warehouseBackend/logs:/app/logs
      - ./warehouseBackend/backups:/app/backups
    depends_on:
      redis:
        condition: service_healthy
    restart: unless-stopped
    stop_grace_period: 20s  # 优雅关闭超时时间，确保数据库正常关闭
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    networks:
      - warehouse-network

  # Redis服务
  redis:
    image: docker.cnb.cool/wtrw09/warehouse/redis:7.2-alpine
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    command: redis-server --appendonly yes --requirepass ${REDIS_PASSWORD:-redis123}
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5
      start_period: 5s
    networks:
      - warehouse-network

volumes:
  redis_data:

networks:
  warehouse-network:
    driver: bridge

