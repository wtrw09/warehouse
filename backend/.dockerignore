# 开发环境文件（虚拟环境必须完全排除）
.venv/
.venv*/
venv/
venv*/
venv313/
env/
env*/
.Python
__pycache__/
*/__pycache__/
**/__pycache__/
*.pyc
*.pyo
*.pyd
pip-log.txt
pip-delete-this-directory.txt

# 测试和调试文件
.tox
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
*.log
.pytest_cache

# 项目配置和IDE文件
.vscode
.idea
*.swp
*.swo
*~

# 系统文件
.DS_Store
Thumbs.db

# 文档和说明文件
说明文件/
*.md

# 备份和临时文件
temp_recovery/
backend.zip

# 构建产物（镜像tar文件和源代码包）
*.tar
*.tar.gz
warehouse-backend-*.tar
warehouse-backend-source.zip

# 构建脚本（不需要包含在镜像中）
build-*.ps1
build-*.sh
fix_encoding.py

# 项目根目录的说明文档（不需要包含）
README.md
说明文件/
*.md

# 备份目录（需要保留，因为代码中需要导入backup模块）
# backup/

# 初始化目录（需要保留，因为代码中需要导入initialize模块）
# initialize/

# 测试和示例文件（如果有的话）
test_*
test-*
example*
sample*

# 临时文件和目录
temp_*
temp-*
tmp*
*.tmp

# 但需要保留template目录（Excel模板文件）
!template/
!template/**

# 字体文件（需要保留，用于PDF生成和报表打印）
# fonts/  # 注释掉，字体文件需要包含在镜像中

# 日志文件（需要保留，用于应用日志记录）
# logs/   # 注释掉，日志目录需要包含在镜像中

# 数据文件（需要保留，包含系统配置和初始化数据）
# data/   # 注释掉，数据文件需要包含在镜像中

# Node.js相关文件（后端不需要Node.js依赖）
package.json
package-lock.json
node_modules/

# Git相关
.git
.gitignore

# Docker相关（避免递归复制）
Dockerfile
docker-compose.yml
DOCKER_DEPLOYMENT.md
*.dockerfile

# 环境配置文件（敏感信息，不应包含在镜像中）
.env
.env.*
*.env